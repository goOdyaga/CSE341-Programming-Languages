Part1:

enter $swipl  current direction to run part1 or part2

after enter above command enter [part1] to run first part1

related function with part1:

print_package_info(X) -> display the information of the packages

print_delivery_person_info(X) -> display the information of the delivery people

check(X) -> display the situation of the packaged

assign_package(Package,Person)-> assign specified package to delivery Person


related function with part2:

classify(SepalLength, SepalWidth, PetalLength, PetalWidth,Class) -> classify the flowers with features